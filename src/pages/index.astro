---
import Layout from "../layouts/Layout.astro";
import RepresentativeSearch from "../components/react/RepresentativeSearch";
import PostalCodeForm from "../components/PostalCodeForm.astro";
---

<Layout title="MP Pressure">
  <RepresentativeSearch client:load />
  <PostalCodeForm text="Enter your postal code" />
</Layout>

<style>
  main {
    margin: var(--margin-base);
    padding: 1rem;
    /* width: 800px; */
    /* max-width: calc(100% - 2rem); */
    color: white;
    font-size: 20px;
    line-height: 1.6;
  }
  h1 {
    font-size: 4rem;
    font-weight: 700;
    line-height: 1;
    text-align: center;
    margin-bottom: 1em;
  }
  .link-card-grid {
    display: grid;
    /* grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr)); */
    gap: 2rem;
    padding: 0;
  }
</style>
